# 编码/开发规范

- 禁止使用cat代替文件写入操作
- 代码commit提交必须要使用对应框架格式化代码
- 重复使用的地址/变量可以直接引用,不要重复编写
- 修改业务逻辑前,先全局搜索同类实现,确保统一修改
- 关键的工具/命令/环境缺失必须停止并报告交给人工处理
- 同一业务逻辑出现2次以上,立即提取公共函数,不询问确认
- 调用链关系:追溯上游调用方和下游依赖,评估改动影响范围
- 修改文件之前一定要先看看类似文件的代码实现,确保风格统一
- 方法内部逻辑:逐行阅读理解实现细节,识别边界条件和状态变更
- 业务上下文:理解代码要解决的实际问题,确保修改符合业务预期
- 测试覆盖:检查现有测试用例,确保修改后测试仍能覆盖核心逻辑
- 限制代码长度一般不超过500-1000行,使用拆分和组件化来解决
- 对比测试需覆盖变更前后状态,验证设置前(正向)/设置后(反向)的行为差异
- 实在无法解决的就使用流程图呈现,结合关键代码片段由人工提供思路来解决
- 一个问题解决了很久才解决的要记录到项目的 `docs/常见问题.md` 文件中
- 禁止使用 VITE_ 开头的环境变量存储敏感信息,因为这样会泄漏,特别是私钥
- 状态/类型等固定取值字段必须用语言原生枚举类型统一为独立文件,禁止字符串硬编码或是低效率编写,应该引用同一配置文件
- 特别注意在 plan 模式下,没有提供方案和人工确认的情况下是禁止直接修改代码的
